---
description: Register a custom LaTeX CV or cover-letter template with the framework
agent: build
---

# /add-template — thin pointer; canonical spec is `.claude/commands/add-template.md`

Follow `.claude/commands/add-template.md` exactly, in order. Captures compile engine, fonts, and page limits; verifies the template compiles; wires it into `/apply`. `--use default` restores stock guidance.
