---
description: Reset candidate profile data back to blank state (DESTRUCTIVE - confirms first)
agent: build
---

# /reset — thin pointer; canonical spec is `.claude/commands/reset.md`

Follow `.claude/commands/reset.md` exactly, in order. Input: $ARGUMENTS (`profile` | `documents` | `all`). Destructive: nothing is deleted until the user explicitly confirms scope.
