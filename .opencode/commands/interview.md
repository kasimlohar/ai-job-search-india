---
description: Build a stage-specific interview prep pack + optional mock interview
agent: build
---

# /interview — thin pointer; canonical spec is `.claude/commands/interview.md`

Follow `.claude/commands/interview.md` exactly, in order. Input: $ARGUMENTS (`<company> [<role>]`, or empty to pick from live tracker rows).

Ground every talking point in the archived submitted materials (`documents/applications/<company>_<role>/`); never invent experience. Verify company claims via independent search before use.
