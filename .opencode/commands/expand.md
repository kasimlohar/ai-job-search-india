---
description: Discover hidden competencies from documents/online presence (additive only)
agent: build
---

# /expand — thin pointer; canonical spec is `.claude/commands/expand.md`

Follow `.claude/commands/expand.md` exactly, in order. Additive only: extends the profile, never modifies existing content, never proposes duplicates of what is already recorded.
