---
description: Generate a self-contained offline HTML dashboard of the application pipeline
agent: build
---

# /html-report — thin pointer; canonical spec is `.claude/commands/html-report.md`

Follow `.claude/commands/html-report.md`. Input: $ARGUMENTS (empty = `reports/application-dashboard.html`; custom path; `--open`). Deep-review lane; composes with `/notion-sync` (glanceable lane).
